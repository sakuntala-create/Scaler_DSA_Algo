func maxSubArray(nums []int) int {
     if len(nums)==0{
		return 0
	}

	curSum := 0
	maxSum := math.MinInt

	for i:=0; i<len(nums) ; i++{
         curSum = curSum+ nums[i]
         
		if maxSum< curSum {
			maxSum = curSum
		}
       

		if curSum < 0{
			curSum = 0
		}
	}

	return maxSum
}
